# semiscan

### Setup
Create a virtual environment:
```
python3 -m venv .env
source .env/bin/activate
```
Install dependencies:
```
pip install --upgrade pip
pip install -r requirements.txt
```

### Usage
Run the framework:
```
python semiscan.py <path-to-data> <output-folder>
```
